GUIA N8N LOCAL - ESTADO DESPUES DE SPRINT 5B.1

La guia anterior fue retirada porque describia un endpoint nominal obsoleto,
AUTOMATION_SECRET y un workflow de correo que ya no deben utilizarse.

Estado actual:

- No existe workflow n8n productivo.
- No debe crearse ni activarse un workflow durante Sprint 5B.1.
- La integracion permanece deshabilitada en desarrollo.
- GET /api/automatizaciones/proximas-citas responde 404.
- El contrato futuro es GET /api/automatizaciones/v1/proximas-citas.
- El endpoint v1 solo funciona en produccion habilitada, con API key hash,
  allowlist de red y rate limit.
- No devuelve pacientes individuales.
- No se configuraron SMTP ni destinatarios.

La especificacion segura del endpoint, variables, rotacion y respuesta agregada
esta en docs/N8N.md.

Sprint 5B.2 debera:

- crear y versionar el workflow;
- configurar la credencial X-CAP-Automation-Key dentro de n8n;
- definir destinatario y proveedor de correo;
- endurecer n8n, Docker, proxy, HTTPS, firewall y trust proxy;
- retirar del Compose la referencia obsoleta a AUTOMATION_SECRET.

No copiar secretos productivos entre computadoras. Git sincroniza codigo y
documentacion, no credenciales ni datos locales de n8n.
